#pragma once

#include <iostream>
#include <fstream>
#include <string>
#include <vector>

using namespace std;

vector<string> getInputStrings(string filename);
vector<int> getInputInts(string filename);